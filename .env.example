# Copy to .env and adjust as needed.
# The MCP server itself should connect with the read-only role.

PGHOST=localhost
PGPORT=5432
PGDATABASE=sales
PGUSER=mcp_readonly
PGPASSWORD=change_me

# Safety limits
MCP_MAX_ROWS=500
MCP_STATEMENT_TIMEOUT_MS=5000
MCP_MAX_QUERY_LENGTH=4000
